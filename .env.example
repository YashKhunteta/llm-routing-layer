# Server Port configuration (optional, defaults to 3000)
PORT=3000

# API key for Gemini models (optional, fallback to simulation if missing)
GEMINI_API_KEY=your_gemini_api_key_here

# API key for OpenAI models (optional, fallback to simulation if missing)
# OPENAI_API_KEY=your_openai_api_key_here
